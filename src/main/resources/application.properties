server.port=${PORT:8035}
eureka.client.serviceUrl.defaultZone=${EUREKA_URL:http://eu:ep@localhost:8761/eureka}
spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=jdbc:${CLEARDB_DATABASE_URL:mysql://b0be2428ed7181:92522959@eu-cdbr-west-03.cleardb.net/heroku_751cf1fd486af60?reconnect=true}&serverTimezone=UTC
spring.application.name=sections
spring.rabbitmq.host=${CLOUDAMQP_URL:localhost}
spring.rabbitmq.username=${RABBITMQ_USER:guest}
spring.rabbitmq.password=${RABBITMQ_PASSWORD:guest}

eureka.instance.hostname=${SECTIONSERVICE_HOSTNAME:localhost}
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true

spring.profiles.active=${PROFILE:}
